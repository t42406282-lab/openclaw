services:
  - type: web
    name: zeroclaw-agent
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: NVIDIA_API_KEY
        sync: false
      - key: ZEROCLAW_GATEWAY_PORT
        value: "8080"
      - key: ZEROCLAW_DATA_DIR
        value: /data
