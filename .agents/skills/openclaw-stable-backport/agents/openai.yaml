interface:
  display_name: "OpenClaw Stable Backport"
  short_description: "Prepare one stable patch release from main fixes"
  default_prompt: "Use $openclaw-stable-backport to discover, assess, and prepare security and reliability backports for one OpenClaw stable release line."
